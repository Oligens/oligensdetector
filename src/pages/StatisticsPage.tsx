import { useEffect,useState } from "react";
import { fmtInt } from "../data";
import { Kpi,MeterBar,PageHead,Reveal,SectionTitle } from "../ui";
interface Stats{analysesCount:number;reportsCount:number;avgAi:number;avgPlagiarism:number;avgProcessingMs:number;totalWords:number;databaseCount:number;databaseDocuments:number;activity:Array<{day:string;analyses_count:number;avg_ai:number}>}
export default function StatisticsPage(){const[stats,setStats]=useState<Stats|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{let active=true;void fetch("/api/stats",{credentials:"include"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error??"Statistiques indisponibles");if(active)setStats(d)}).catch(e=>{if(active)setError(e instanceof Error?e.message:"Statistiques indisponibles")});return()=>{active=false}},[]);const max=Math.max(1,...(stats?.activity??[]).map(a=>a.analyses_count));return <><PageHead kicker="Statistiques calculées depuis les données du compte" title="Statistiques"/><div className="grid grid-cols-2 gap-4 lg:grid-cols-4"><Kpi label="Analyses cumulées" value={stats?fmtInt(stats.analysesCount):"—"} sub="table analyses"/><Kpi label="Risque IA moyen" value={stats?stats.avgAi.toLocaleString("fr-FR",{maximumFractionDigits:1}):"—"} unit="%" sub="données réellement enregistrées"/><Kpi label="Plagiat moyen" value={stats?stats.avgPlagiarism.toLocaleString("fr-FR",{maximumFractionDigits:1}):"—"} unit="%" sub="données réellement enregistrées"/><Kpi label="Temps moyen" value={stats&&stats.avgProcessingMs?((stats.avgProcessingMs)/1000).toLocaleString("fr-FR",{maximumFractionDigits:2}):"—"} unit="s" sub="processing_time_ms"/></div>{error&&<div className="glass mt-5 rounded-xl border border-rose-400/25 p-4 text-[12.5px] text-rose-300">{error}</div>}<div className="mt-5 grid grid-cols-1 gap-5 xl:grid-cols-2"><Reveal><section className="glass rounded-2xl p-5"><SectionTitle title="Activité des 30 derniers jours" right={<span className="font-mono text-[10px] text-ink-500">API Neon</span>}/>{stats?.activity.length?<div className="space-y-3">{stats.activity.map(a=><div key={a.day}><div className="mb-1 flex justify-between text-[11px]"><span className="font-mono text-ink-500">{a.day}</span><span className="text-ink-200">{a.analyses_count} analyse{a.analyses_count>1?"s":""} · IA {Number(a.avg_ai).toLocaleString("fr-FR",{maximumFractionDigits:1})}%</span></div><MeterBar value={(a.analyses_count/max)*100} height={5} color="linear-gradient(90deg,#2b4fa6,#5b8def)"/></div>)}</div>:<p className="py-10 text-center text-[12.5px] text-ink-500">Aucune analyse réelle enregistrée sur cette période.</p>}</section></Reveal><Reveal delay={100}><section className="glass rounded-2xl p-5"><SectionTitle title="Volume réellement stocké"/><div className="grid grid-cols-2 gap-3"><div className="glass-soft rounded-xl p-4"><p className="label-caps text-ink-500">Mots analysés</p><p className="mt-2 font-display text-2xl font-bold text-ink-100">{stats?fmtInt(stats.totalWords):"—"}</p></div><div className="glass-soft rounded-xl p-4"><p className="label-caps text-ink-500">Bases institutionnelles</p><p className="mt-2 font-display text-2xl font-bold text-ink-100">{stats?fmtInt(stats.databaseCount):"—"}</p></div><div className="glass-soft rounded-xl p-4 col-span-2"><p className="label-caps text-ink-500">Documents institutionnels</p><p className="mt-2 font-display text-2xl font-bold text-ink-100">{stats?fmtInt(stats.databaseDocuments):"—"}</p></div></div></section></Reveal></div></>}
