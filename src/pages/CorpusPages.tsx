import { useRef,useState } from "react";
import { Link,useNavigate,useParams,useSearchParams } from "react-router-dom";
import { IconEye,IconFile,IconFolder,IconScan,IconTrash,IconUpload } from "../components/icons";
import { fmtInt } from "../data";
import { PageHead,Pill,Reveal } from "../ui";
import { useCorpus } from "../state/CorpusContext";
import { useAnalysis } from "../state/AnalysisContext";
const kindCls:Record<string,string>={pdf:"border-rose-400/35 bg-rose-400/10 text-rose-400",docx:"border-azure-400/35 bg-azure-400/10 text-azure-300",txt:"border-ink-400/30 bg-white/5 text-ink-300",md:"border-jade-400/30 bg-jade-400/10 text-jade-400",rtf:"border-ink-400/30 bg-white/5 text-ink-300"};
export default function CorpusListPage(){const{folders,addFolder,deleteFolder}=useCorpus();const{toast}=useAnalysis();const navigate=useNavigate();const[name,setName]=useState("");const[theme,setTheme]=useState("");const[busy,setBusy]=useState(false);const create=async()=>{if(!name.trim()||busy)return;setBusy(true);try{const folder=await addFolder(name.trim(),theme.trim());setName("");setTheme("");toast("Dossier créé",`« ${folder.name} » est enregistré dans Neon.`);navigate(`/corpus/${folder.id}`)}catch(e){toast("Création impossible",e instanceof Error?e.message:"Erreur serveur.")}finally{setBusy(false)}};const totalDocs=folders.reduce((a,f)=>a+f.files.length,0);return <><PageHead kicker="Organisation des bases institutionnelles" title="Corpus & dossiers" actions={<span className="rounded-lg border border-gold-400/30 bg-gold-400/10 px-3 py-1.5 font-mono text-[12px] font-bold text-gold-300">{folders.length} bases · {totalDocs} documents</span>}/><Reveal><section className="glass mb-5 rounded-2xl p-5"><p className="label-caps mb-3 text-ink-500">Créer une base de comparaison</p><div className="flex flex-wrap gap-2.5"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nom de la base…" className="min-w-[220px] flex-1 rounded-lg border border-white/10 bg-night-900/60 px-3.5 py-2.5 text-[13px] text-ink-200 outline-none"/><input value={theme} onChange={e=>setTheme(e.target.value)} placeholder="Thématique…" className="w-44 rounded-lg border border-white/10 bg-night-900/60 px-3.5 py-2.5 text-[13px] text-ink-200 outline-none"/><button onClick={()=>void create()} disabled={!name.trim()||busy} className="btn-gold px-4 py-2.5 text-[13px] disabled:opacity-40">{busy?"Création…":"+ Créer"}</button></div><p className="mt-2.5 text-[11px] text-ink-500">Les dossiers et documents sont stockés dans institutional_databases via l'API authentifiée.</p></section></Reveal><div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">{folders.map(f=>{const mots=f.files.reduce((a,d)=>a+d.mots,0);return <Reveal key={f.id}><article className="glass card-hover flex h-full flex-col rounded-2xl p-5"><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl border border-gold-400/25 bg-gold-400/[0.08] text-gold-300"><IconFolder className="h-5 w-5"/></span><Pill tone="gold">{f.theme||"Sans thème"}</Pill></div><h3 className="mt-3.5 text-[13.5px] font-semibold text-ink-100">{f.name}</h3><p className="mt-1 font-mono text-[10.5px] text-ink-500">{f.files.length} document{f.files.length>1?"s":""} · {fmtInt(mots)} mots · {f.createdAt}</p><div className="mt-4 flex gap-2"><button onClick={()=>navigate(`/corpus/${f.id}`)} className="btn-ghost flex-1 px-2 py-1.5 text-[11.5px]"><IconEye className="h-3.5 w-3.5"/> Ouvrir</button><button onClick={()=>navigate(`/corpus/scan?id=${f.id}`)} className="btn-gold flex-1 px-2 py-1.5 text-[11.5px]"><IconScan className="h-3.5 w-3.5"/> Scanner</button><button onClick={()=>deleteFolder(f.id)} className="grid h-8 w-8 place-items-center rounded-lg border border-white/10 text-ink-500 hover:text-rose-400" aria-label="Supprimer"><IconTrash className="h-4 w-4"/></button></div></article></Reveal>})}</div>{folders.length===0&&<Reveal><div className="glass mt-4 grid place-items-center rounded-2xl border-dashed px-6 py-16 text-center"><IconFolder className="h-10 w-10 text-ink-500"/><p className="mt-3 text-[13px] text-ink-400">Aucune base institutionnelle réelle.</p></div></Reveal>}</>}
export function CorpusDetailPage(){const{id}=useParams();const{getFolder,addFile,removeFile}=useCorpus();const{toast}=useAnalysis();const navigate=useNavigate();const ref=useRef<HTMLInputElement>(null);const[importing,setImporting]=useState(false);const folder=id?getFolder(id):undefined;const importFiles=async(files:FileList|null)=>{if(!files||!folder)return;setImporting(true);try{for(const file of Array.from(files)){const{extractTextFromFile}=await import("../lib/detector/textExtractor");const text=await extractTextFromFile(file);if(!text.trim()){toast("Fichier vide",file.name);continue}addFile(folder.id,{name:file.name,kind:(file.name.split(".").pop()?.toLowerCase()??"txt") as any,sizeKo:Math.max(1,Math.round(file.size/1024)),mots:text.trim().split(/\s+/).length,text});toast("Document ajouté",`${file.name} est indexé dans Neon.`)}}catch(e){toast("Import impossible",e instanceof Error?e.message:"Erreur d'extraction.")}finally{setImporting(false)}};if(!folder)return <div className="glass grid place-items-center rounded-2xl border-dashed px-6 py-20 text-center"><IconFolder className="h-12 w-12 text-ink-500"/><p className="mt-4 text-[15px] font-bold text-ink-100">Base introuvable</p><Link to="/corpus" className="btn-gold mt-5 px-4 py-2.5 text-[13px]">Retour aux bases</Link></div>;const mots=folder.files.reduce((a,d)=>a+d.mots,0);return <><PageHead kicker={`Base institutionnelle · ${folder.theme||"sans thème"}`} title={folder.name} actions={<div className="flex gap-2"><Link to="/corpus" className="btn-ghost px-3.5 py-2 text-[12.5px]">← Bases</Link><button onClick={()=>navigate(`/corpus/scan?id=${folder.id}`)} className="btn-gold px-4 py-2 text-[12.5px]"><IconScan className="h-4 w-4"/> Scanner</button></div>}/><div className="grid grid-cols-2 gap-4 lg:grid-cols-4"><Stat label="Documents" value={String(folder.files.length)}/><Stat label="Mots indexés" value={fmtInt(mots)}/><Stat label="Volume" value={`${fmtInt(folder.files.reduce((a,d)=>a+d.sizeKo,0))} Ko`}/><Stat label="Thématique" value={folder.theme||"—"}/></div><Reveal delay={100}><section className="glass mt-5 rounded-2xl p-5"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><h2 className="font-display text-[13px] font-semibold text-ink-100">Documents</h2><div><button onClick={()=>ref.current?.click()} className="btn-gold px-4 py-2 text-[12.5px]"><IconUpload className="h-4 w-4"/> {importing?"Import…":"Ajouter"}</button><input ref={ref} type="file" multiple accept=".pdf,.docx,.doc,.txt,.md,.rtf" className="hidden" onChange={e=>void importFiles(e.target.files)}/></div></div>{folder.files.length?<ul className="divide-y divide-white/[0.05]">{folder.files.map(d=><li key={d.id} className="flex items-center gap-3 rounded-lg px-2 py-3"><span className={`grid h-10 w-10 place-items-center rounded-lg border font-mono text-[9px] font-bold uppercase ${kindCls[d.kind]??kindCls.txt}`}>{d.kind}</span><div className="min-w-0 flex-1"><p className="truncate text-[13px] font-semibold text-ink-100">{d.name}</p><p className="font-mono text-[10.5px] text-ink-500">{fmtInt(d.mots)} mots · {fmtInt(d.sizeKo)} Ko</p></div><button onClick={()=>removeFile(folder.id,d.id)} className="grid h-8 w-8 place-items-center rounded-lg border border-white/10 text-ink-500 hover:text-rose-400"><IconTrash className="h-4 w-4"/></button></li>)}</ul>:<p className="py-10 text-center text-[12.5px] text-ink-500">Aucun document réel dans cette base.</p>}</section></Reveal></>}
function Stat({label,value}:{label:string;value:string}){return <div className="glass rounded-xl p-4"><p className="label-caps text-ink-500">{label}</p><p className="mt-2 font-display text-xl font-bold text-ink-100">{value}</p></div>}
export function CorpusScanPage(){const[params]=useSearchParams();const id=params.get("id");const folder=useCorpus().getFolder(id??"");const{toast,startScan}=useAnalysis();const[text,setText]=useState("");const[busy,setBusy]=useState(false);const run=()=>{if(!folder||text.trim().length<100)return;setBusy(true);try{startScan({name:`Corpus_${folder.name}.txt`,text,sizeKo:Math.max(1,Math.round(text.length/1024))},{redirectTo:"/analyses"})}finally{setBusy(false)}};return <><PageHead kicker={`Scan ciblé · ${folder?.name??"base introuvable"}`} title="Scanner contre le corpus" actions={<Link to={id?`/corpus/${id}`:"/corpus"} className="btn-ghost px-3.5 py-2 text-[12px]">Retour</Link>}/><section className="glass rounded-2xl p-5"><p className="text-[12.5px] text-ink-400">Le scan utilise le quota du compte et persiste le résultat dans Neon.</p><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Collez le texte à analyser…" className="mt-4 h-48 w-full rounded-xl border border-white/10 bg-night-900/60 p-3 text-[12.5px] text-ink-200 outline-none"/><button disabled={!folder||busy||text.trim().length<100} onClick={run} className="btn-gold mt-4 px-4 py-2.5 text-[12.5px] disabled:opacity-40">{busy?"Analyse…":`Analyser contre ${folder?.name??"le corpus"}`}</button>{!folder&&<p className="mt-3 text-[12px] text-rose-300">Base introuvable.</p>}</section></>}
